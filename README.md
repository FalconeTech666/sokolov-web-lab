# sokolov-web-lab
A practical web development laboratory built with Flask. Features include user authentication (SQLAlchemy), external API integrations (Weather, Currency, NBRB), internationalization (i18n), dark mode, and responsive UI.
